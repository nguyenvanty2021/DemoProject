export enum RoutesRedirect {
    Dashboard = '/',
    DeliveryOrders = "/delivery/orders",
    DeliveryImportOrders = "/delivery/import-orders",
    DeliveryRoutes = "/delivery/routes",
    CommonCustomers = "/common/customers",
    CommonPlaces = "/common/places",
    CommonStandardCodes = "/common/standard-codes",
    CommonDrivers = "/common/drivers",
    AdminUsers = "/admin/users",
  }